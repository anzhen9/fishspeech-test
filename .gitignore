# ============================================================
# 环境与依赖（体积巨大，且不具可移植性）
# ============================================================
# embeddable Python 3.11 + 全部 site-packages (~5.6 GB)
.tools/

# ============================================================
# 模型权重（从 HuggingFace / hf-mirror 下载，不入库）
# ============================================================
# FishSpeech 1.5 + Whisper large-v3 + ECAPA-TDNN (~5.8 GB)
models/

# ============================================================
# 运行时产物
# ============================================================
# 每次请求/测试的输出：wav / srt / mp4 / report.json
outputs/
# uvicorn 与 loguru 日志
logs/
# TTS 结果缓存（运行时自动重建）
data/tts_cache/

# ============================================================
# 测试数据（514 MB，按需用 tests/prepare_data.py 重新生成）
# ============================================================
data/testset/
# FLEURS 原始音频缓存（281 MB，由 tests/prepare_data.py 下载）
data/fleurs_zh/
# 注册音色为示例音频，不入库（同名 .txt 保留作格式示例）
data/voices/*.wav

# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ============================================================
# 编辑器与本地工具数据（含本机绝对路径，不入库）
# ============================================================
.vscode/
.idea/

# ============================================================
# 打包与临时文件
# ============================================================
*.tar.gz
*.tmp
*.log
.DS_Store
Thumbs.db
