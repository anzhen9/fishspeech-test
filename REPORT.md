# 统一语音处理服务（USS）—— 三项功能端到端测试报告

> 基于 FishSpeech 1.5 · NVIDIA GeForce RTX 3060 12GB
> 报告生成时间：2026-09-01

---

## 0. 测试环境

| 项目 | 值 |
|---|---|
| GPU | NVIDIA GeForce RTX 3060（12.0 GB，Ampere sm_86，bf16 原生支持） |
| CPU / OS | Windows 10 x64 |
| Python | 3.11.9（embeddable，64bit） |
| PyTorch | 2.6.0+cu124 |
| CUDA / cuDNN | 12.4 / 9.1.0 |
| faster-whisper | 1.1.0（CTranslate2 4.5.0 后端） |
| speechbrain | 1.0.2（ECAPA-TDNN，VoxCeleb 预训练） |
| FishSpeech | 1.5（dual_ar LLaMA + Firefly VQGAN，44.1 kHz 输出） |
| 服务监听 | `http://127.0.0.1:8080` |

**测试数据集**：Google FLEURS `zh-CN` dev 子集，本地筛选后共 30 条语音（总时长 266.9 s），
克隆子集由 ECAPA 对 222 条候选贪心聚类（阈值 0.55）得到 5 个说话人簇，选取 4 个簇，
每人 2 段（`ref` 用于注册、`held` 为同人留出真值，用于排除"复刻同一段录音"的作弊）。

---

## 1. 结论速览

| 能力 | 核心指标 | 实测值 | 判定 |
|---|---|---|---|
| **字幕识别 ASR** | 字准确率（1 − CER） | **97.59 %** | 优秀 |
| | 段起点偏差（带符号） | **−0.7 ms** | 优秀 |
| | 实时率 RTF | **0.132**（7.6× 实时） | 优秀 |
| **语音克隆** | 目标说话人相似度（留出段） | **0.589**（阈值 0.55） | 合格 |
| | 与其他说话人相似度 | 0.217 | — |
| | 区分度差值（margin） | **+0.371** | 优秀 |
| | 合成内容字准确率 | **100 %** | 优秀 |
| **口型同步** | 起口偏差（带符号） | **−5.5 ms** | 优秀 |
| | 帧级口型-语音一致性 F1 | **0.939** | 良好 |
| | 静音段闭口率 | 0.974 | 优秀 |
| | 有声段张嘴率 | 0.901 | 良好 |
| | 收口偏差（带符号） | −73.8 ms | 待改进 |
| **并发承载** | 混合负载吞吐上限 | **~18 req/min**（不受并发度影响） | 见 §4.5 |
| | `/health` 探针延迟（并发 8） | **104 ms**（修复前 31 027 ms） | 已修复 |
| | TTS 纯负载并发上限 | **~6 人**（RTF 1.95 是瓶颈） | 瓶颈 |

---

## 2. 能力一：字幕识别（ASR）

### 2.1 测试设计

- **输入**：30 条 FLEURS 中文语音（8–11 s/条）
- **真值**：数据集原文（已剔除拉丁转写标注后参与 CER 计算）
- **模型**：faster-whisper `large-v3`，`compute_type=float16`，`beam_size=5`，`language=zh`，`word_timestamps=True`
- **VAD**：Silero VAD（faster-whisper 内置），`speech_pad_ms=30`、`min_silence_duration_ms=300`
- **时间对齐真值**：**Silero VAD 独立求段**（`speech_pad_ms=0`），与 Whisper 的时间戳预测头相互独立，避免自证

### 2.2 实测结果（30/30 全部成功，0 失败）

| 指标 | 值 | 说明 |
|---|---|---|
| `cer_mean` | **0.0241** | 归一化后字错误率 |
| `char_accuracy_mean` | **97.59 %** | 1 − CER |
| 句级完全匹配率 | 56.67 %（17/30） | 整句一字不差的比例 |
| `cer_raw_mean` | 0.0575 | 未做标点/数字规范化前的原始 CER |
| `onset_bias_mean_ms` | **−0.7 ms** | 段起点带符号偏差（负=偏早） |
| `onset_err_mean_ms` | 37.3 ms | 起点绝对误差均值 |
| `onset_within_100ms` | **96.7 %** | 起点落在 ±100 ms 内的比例 |
| `voiced_coverage_mean` | **0.9754** | 预测语音段对真实语音区的覆盖率 |
| `rtf_mean` | **0.1318** | 266.9 s 音频总墙钟 32.76 s |
| 峰值显存 | 3.2 GB（large-v3 fp16 权重 + 编码激活） | |

### 2.3 典型错误样本

| 样本 | CER | 真值 | 识别结果 |
|---|---|---|---|
| asr_23 | 0.143 | 然而，我们百分之八十的商品… | 然而我们**80%**的商品… |
| asr_26 | 0.111 | 请给予此地**应得**的尊严、**庄严**和尊重。 | 请给予此地**英德**的尊严、**庄重**和尊重。 |
| asr_02 | 0.083 | …观看博物馆的人工制品，参观**水族馆**，或者欣赏**精美**的艺术品。 | …参观**水果肉馆**，或者欣赏**精品**的艺术品。 |
| asr_01 | 0.046 | 这是马特利 (Martelly) 四年来第五次入选海地临时选举委员会 (CEP)。 | 这是马特利第四年来第五次入选海地临时选举委员会 |

**错误模式**：几乎全部是**同音/近音字替换**（应得→英德、水族馆→水果肉馆、栗棕色→立宗色），
以及**数字与符号规范化差异**（百分之八十→80%、括号内容丢弃）。
这些对字幕可读性影响很小（不影响理解），但会拉低句级完全匹配率。
标点差异（，↔,）已通过归一化从 CER 中剔除（0.0575 → 0.0241）。

### 2.4 关键发现与修复

**发现：段起点系统性偏早 −370 ms（30/30 全负，最差 −420 ms）**

根因不在 Whisper，而在 faster-whisper 的 VAD 默认值 `speech_pad_ms=400`：
它会把检测到的语音段**向前**填充 400 ms 静音再送进 Whisper，
而 Whisper 面对前置静音时倾向于把首段时间戳直接预测到 0。

对照实验（同一条样本，能量 VAD 真值起点 0.520 s）：

| `speech_pad_ms` | 预测起点 | 误差 |
|---|---|---|
| 400（默认） | 0.110 s | −410 ms |
| 0 | 0.510 s | −10 ms |
| **30（采用）** | — | **−0.7 ms（30 条均值）** |

保留 30 ms 余量是防止 Silero VAD 本身轻微切边。这一改动把 `onset_within_100ms` 从 **0.0 提升到 0.967**。

---

## 3. 能力二：语音克隆（Voice Cloning）

### 3.1 测试设计

采用 **ref / held 双段 + 4×4 注册矩阵** 的严格设计：

- 每个说话人提供两段音频：`ref`（10 s，用于注册/提取 prompt）与 `held`（同人另一段，**不参与合成**，作为真值）
- 合成固定目标句「今天的天气非常不错，我们一起去公园散步吧。」（与参考音频内容完全不同）
- 评估三个相似度：
  - `cos_ref`：合成音频 vs 注册段
  - `cos_held`：合成音频 vs **留出段**（更严格——无法靠"复刻同一段录音"刷分）
  - `cos_other`：合成音频 vs 其余 3 个说话人
- **4×4 注册矩阵**：把 4 个合成音频与 4 个说话人的留出段两两比对，取每行的 argmax 作为识别结果，算 Top-1 命中率
- **参考音频时长**：仅 10 s / 人（实际 9.3–10.5 s）

**模型**：FishSpeech 1.5，bf16，`top_p=0.7`、`temperature=0.7`、`repetition_penalty=1.2`、`chunk_length=150`、`seed=42`

### 3.2 实测结果（4/4 全部成功，0 失败）

| 指标 | 值 |
|---|---|
| `cos_ref_mean` | **0.6222** |
| `cos_held_mean` | **0.5885**（> 0.55 同人阈值） |
| `cos_other_mean` | 0.2172 |
| `margin_vs_other_mean` | **+0.3713** |
| `same_speaker_rate_ref` | 0.75 |
| `same_speaker_rate_held` | 0.75 |
| `top1_enroll_acc` | **0.75**（3/4） |
| `gen_cer_mean` | **0.0** → 合成内容字准确率 **100 %** |
| `rtf_mean` | 1.953 |
| 峰值显存 | 1654.8 MB |

### 3.3 分说话人明细

| 说话人 | 性别 | cos_ref | cos_held | cos_other | margin | Top-1 | 内容 CER |
|---|---|---|---|---|---|---|---|
| cluster0（clone_01_F） | F | 0.659 | 0.566 | 0.145 | +0.421 | ✅ 命中 | 0.0 |
| cluster1（clone_02_M） | M | **0.771** | **0.674** | 0.145 | **+0.529** | ✅ 命中 | 0.0 |
| cluster2（clone_03_M） | M | 0.379 | 0.470 | 0.305 | +0.165 | ❌ 误判为 cluster3 | 0.0 |
| cluster3（clone_04_M） | M | 0.680 | 0.645 | 0.275 | +0.370 | ✅ 命中 | 0.0 |

**4×4 注册矩阵**（行=合成音频的说话人，列=与哪个说话人的留出段最像）：

| ↓合成 \ 比对→ | cluster0 | cluster1 | cluster2 | cluster3 | argmax |
|---|---|---|---|---|---|
| cluster0 | **0.5656** | 0.3914 | 0.0021 | 0.0412 | ✅ cluster0 |
| cluster1 | 0.2354 | **0.6740** | 0.0363 | 0.1625 | ✅ cluster1 |
| cluster2 | 0.1434 | 0.2187 | 0.4696 | **0.5514** | ❌ cluster3 |
| cluster3 | 0.0694 | 0.1641 | **0.5900** | **0.6447** | ✅ cluster3 |

### 3.4 结果分析

- **3/4 稳定克隆成功**：`cos_held` 0.645–0.674，明显高于 0.55 同人阈值，且 `margin` 均在 +0.37 以上。
- **cluster2 是唯一失败案例**：`cos_held` 0.470 低于阈值，且被误判到 cluster3（0.551）。
  观察其 `cos_ref=0.379` 也显著低于其他三人（0.66–0.77），说明**问题出在参考音频本身或该说话人的音色可辨识度**，
  而不是合成环节。cluster2 与 cluster3 的留出段互相相似度偏高（矩阵中 cluster3 行 cluster2 列 = 0.590），
  两者很可能是**同一聚类过程中的近邻说话人**（都是男声、录音条件相近），导致边界模糊。
- **内容准确率 100 %**：4 条合成音频的 ASR 回识别结果与目标句完全一致，说明 in-context 克隆没有损害可懂度。
- **RTF 1.95**（慢于实时）：FishSpeech 是 dual-AR 自回归解码，3060 上没有算子融合优化，
  生成 4.7 s 音频需约 9.2 s。这是 3060 上该模型的固有代价。

### 3.5 关键实现要点

FishSpeech 1.5 的克隆是 **in-context learning**：参考音频经 VQ 编码得到 `prompt_tokens`，
作为 LLaMA 的前缀。**`prompt_text` 与 `prompt_tokens` 必须成对提供**，
否则引擎会静默置 `use_prompt=False`，退化成随机音色——这是一个很容易踩且没有任何报错的坑。
本服务在 `service/engines/clone.py` 中强制校验了这一对偶关系。

---

## 4. 能力三：口型同步（Lip Sync）

### 4.1 测试设计

**管线**：音频 → Whisper 词级时间戳 → pypinyin 拆声母/韵母 → Rhubarb 九宫 viseme（A–H + X）→ 时间轴 → 25 fps 渲染

- **声韵母时长权重**：声母 0.30 / 韵母 0.70（汉语韵母承载主要响度与口型开合）
- **静音处理**：静音 > 60 ms 插入闭口 viseme X；相邻同口型 < 40 ms 合并防抖
- **尾部延展**：末词结束沿能量包络延展，上限 500 ms
- **渲染**：512×512 MP4，25 fps

**评测指标**（注意：这里踩过一个指标设计的坑，见 4.4）：

| 指标 | 定义 |
|---|---|
| 起口/收口偏差 | 首个/末个非 X 口型时刻 − Silero VAD 段起止时刻（带符号） |
| 帧级 F1 | 逐帧判定"张嘴"与"语音活跃"是否一致，算 P/R/F1 |
| 张嘴率 | 语音活跃帧中口型张开的比例 |
| 闭口率 | 静音帧中口型闭合的比例 |
| AV 偏移 | 口型开合曲线与能量包络的互相关峰值（含抛物线亚帧插值） |

### 4.2 实测结果（30/30 全部成功，0 失败，5 个样本额外渲染了 MP4）

| 指标 | 值 |
|---|---|
| `onset_bias_mean_ms` | **−5.5 ms**（绝对误差均值 10.7 ms） |
| `onset_within_100ms_rate` | **100 %** |
| `offset_bias_mean_ms` | −73.8 ms（绝对误差均值 108.1 ms） |
| `frame_f1_mean` | **0.939** |
| `voiced_open_rate_mean` | 0.901 |
| `silence_closed_rate_mean` | **0.974** |
| `av_offset_mean_ms` | −10.8 ms（绝对均值 106.8 ms，P95 228.9 ms） |
| `peak_corr_mean` | 0.494 |
| `timeline_coverage_mean` | **1.0**（时间轴完整覆盖整段音频，无空洞） |
| `switches_per_sec_mean` | 5.57 次/秒 |
| `syllables_per_sec_mean` | 3.27 字/秒（对照） |
| 墙钟 | 46.62 s（30 条） |

### 4.3 最佳 / 最差样本

| 样本 | 帧 F1 | 起口偏差 | 收口偏差 | AV 偏移 |
|---|---|---|---|---|
| asr_09（最佳） | 0.985 | −14 ms | −32 ms | −76 ms |
| asr_01 | 0.980 | −4 ms | −64 ms | +190 ms |
| asr_11 | 0.978 | −32 ms | −40 ms | −82 ms |
| … | | | | |
| asr_19 | 0.839 | −14 ms | −216 ms | −71 ms |
| asr_23 | 0.821 | −8 ms | −144 ms | +215 ms |
| asr_22（最差） | 0.813 | −6 ms | −136 ms | +87 ms |

**规律**：起口一致性很好（全部在 ±100 ms 内，均值仅 −5.5 ms），
**收口是主要短板**（均值 −73.8 ms，最差 −216 ms）。最差样本的共性不是"张错嘴"，
而是**尾音处提前闭口**——帧 F1 0.813 对应的是"整段末尾 100–200 ms 该张嘴却闭了"。

### 4.4 关键发现与修复

**发现一：指标本身不成立 —— 边界 F1 恒接近 0（0.0–0.334）**

最初的评测想把每个口型的起止边界与 VAD 段边界做配对，算精确率/召回率。
结果 30 条样本的 F1 全部接近 0，一度以为口型生成有严重问题。

排查后发现是**粒度失配**：口型是**音节级**（实测 5.57 次切换/秒），
而 VAD 段是**语句级**（5 s 音频只有 1–3 段）。
用 ±50 ms 容差去配对两个差一个数量级的序列，配对数天然为 0。

**修复**：改为**帧级一致性**——把口型开合度与 VAD 语音活跃标记都重采样到 25 fps 逐帧比对。
这直接反映了"观众感知到的同步性"，且与粒度无关。改后 F1 = 0.939，与主观观感吻合。

**发现二：收口提前 186 ms（30/30 全负）**

Whisper 的末词 `end` 时间戳只到"**可辨识语音**"结束，其后还有 100–400 ms 的**能量衰减尾巴**
（尾音、气声、混响），这段时间嘴其实还是张的。

**修复**：新增 `_extend_tail()`，在末词 `end` 之后沿短时能量包络扫描，
把最后一个口型延展到能量真正跌到本底噪声的时刻（上限 `max_tail_ms=500`）。
改善：−186 ms → **−73.8 ms**。剩余误差主要来自 Whisper 对句末拖长音的截断倾向，属于模型固有属性。

**发现三：AV 偏移指标的分辨率陷阱**

25 fps 下整数帧滞后只有 40 ms 分辨率，直接取互相关峰值只能给 ±20 ms 的量化误差，
导致 `sync_within_40ms_rate` 恒为 0（看着像"完全不同步"，实际是假象）。

**修复**：在互相关峰值处加**抛物线亚帧插值**：

```python
denom = (y0 - 2 * y1 + y2)
delta = 0.5 * (y0 - y2) / denom
sub_lag = best_lag + delta      # 连续滞后估计
```

同时报告 `peak_corr`（0.494）。需要说明：**峰值相关 0.49 偏低是合理的**，
因为口型开合是**离散的音符序列**（方波状），能量包络是连续的模拟信号，
两者波形形态本就不同；相关值低不等于不同步——此时**带符号偏移（−10.8 ms）才是更可信的判据**。

---

## 4.5 并发与 3060 承载能力（重点）

前三项测试都是**串行**执行的，只能反映"单次调用效果"。
但作为服务，真正的问题是：**多人多任务同时打进来会怎样？**
本节给出实测答案。

### 4.5.1 压测设计

- **虚拟用户**：N 个线程独立发请求，模拟 N 个并发用户
- **混合负载**（贴近真实使用比例）：ASR 40% / TTS 30% / 口型 15% / 健康探针 15%
- **并发级别**：1 / 2 / 4 / 8，每级 32 个请求
- **测试音频**：8 个不同文件轮询，模拟不同用户上传不同内容
- **采集**：吞吐、延迟分位、排队时间、成功率、显存采样、"轻任务饿死比"

> 脚本：`tests/test_concurrency.py`、`tests/test_capacity.py`、对比工具 `tests/compare_concurrency.py`

### 4.5.2 修复前基线：服务对外表现为"卡死"

| 并发 | 吞吐 req/min | 加速比 | 效率 | P50 延迟 | 排队均值 | `/health` 延迟 | 饿死比 | 成功率 |
|---|---|---|---|---|---|---|---|---|
| 1 | 17.66 | 1.00x | 100% | 1.35 s | 0.23 s | 3 ms | 0.6x | 100% |
| 2 | 17.06 | 0.97x | 48% | 3.17 s | 3.52 s | **1 318 ms** | **260x** | 100% |
| 4 | 20.39 | 1.15x | 29% | 12.15 s | 9.37 s | **17 939 ms** | **3 534x** | 88% |
| 8 | 18.99 | 1.08x | 13% | 22.18 s | 20.07 s | **31 027 ms** | **6 113x** | 84% |

两个致命信号：

1. **吞吐完全不随并发提升**（17.7 → 19.0，几乎持平）。并发 8 的效率只有 13%。
2. **`/health` 探针要等 31 秒**（饿死 6 113 倍）。这意味着服务在高负载下
   对外**表现为完全无响应** —— 心跳检测会失败、负载均衡会摘除节点、
   前端会显示"服务不可用"，哪怕 GPU 其实一直在正常干活。

### 4.5.3 根因：`async def` 里的同步调用独占了事件循环

FastAPI 的 `async def` 端点运行在 asyncio 事件循环上。
**在 `async def` 里直接调用同步阻塞函数，会独占整个事件循环** ——
一个 8 秒的 TTS 请求期间，同一时刻到达的其它所有请求（包括只需 2 ms 的 `/health`）
全部只能在队列里干等。

原代码正是这个形态：

```python
@app.post("/v1/tts")
async def api_tts(...):
    engine = clone_mod.get_tts()          # 同步，可能触发数秒的模型加载
    res = engine.synthesize(...)          # 同步，~9 秒，期间事件循环完全阻塞
```

延迟数据也印证了这点 —— 并发 4 时延迟呈
`1.83 / 3.72 / 5.44 / 7.25 s` 的**等差阶梯**，公差恰好等于单请求耗时，
说明请求被逐个串行处理。

### 4.5.4 修复：把阻塞调用移出事件循环 + 按模型分组串行

新增 `service/concurrency.py`：

```python
async def run_gpu(kind, fn, *args, **kwargs):
    async with _total_sem():          # 全局额度，防止显存超限
        async with _group_sem(kind):  # 同类模型串行，异类可并行
            return await run_in_threadpool(fn, *args, **kwargs)
```

三条设计原则：

| 原则 | 做法 | 理由 |
|---|---|---|
| **不阻塞事件循环** | `run_in_threadpool` 执行阻塞推理 | 轻请求不再被重请求饿死 |
| **同类模型串行** | `asr`/`tts`/`speaker` 各自信号量 = 1 | 同一模型实例并发推理会共享 CUDA context 与 KV cache，产生错误输出甚至崩溃 |
| **异类模型并行** | 全局额度 `USS_GPU_SLOTS=2` | 不同模型权重与显存区独立，可重叠以提升 GPU 利用率；额度限制防止 OOM |

所有端点（`/v1/asr`、`/v1/tts`、`/v1/lipsync`、`/v1/speaker/similarity`、`/v1/pipeline`）
均已改为该形态；`/v1/status` 新增 `queue` 字段暴露各分组排队深度。

### 4.5.5 修复后：响应性恢复，但吞吐上限不变

| 并发 | 吞吐 前→后 | P50 前→后 | `/health` 前→后 | 饿死比 前→后 | 成功率 前→后 |
|---|---|---|---|---|---|
| 1 | 17.7 → 17.9 | 1.35s → 1.36s | 3ms → 1ms | 0.6x → 0.3x | 100% → 100% |
| 2 | 17.1 → 17.6 | 3.17s → **2.03s** | 1 318ms → **2ms** | 260x → **0.4x** | 100% → 100% |
| 4 | 20.4 → 17.6 | 12.15s → **8.38s** | 17 939ms → **15ms** | 3 534x → **4.0x** | 88% → **100%** |
| 8 | 19.0 → 17.8 | 22.18s → 21.31s | 31 027ms → **104ms** | 6 113x → **27.8x** | 84% → **100%** |

**结论必须说清楚**：

- ✅ **响应性问题彻底解决**。`/health` 从 31 秒降到 104 毫秒（**298 倍**），
  成功率 84% → 100%。服务不再"假死"，心跳、状态查询、文件下载始终秒回。
- ❌ **吞吐上限没有提升**（约 18 req/min 不变）。
  这是**物理上限，不是 bug**：单张 3060 的 GPU 算力就这么多，
  推理本身必须串行，增加并发只会让队列变长，不会让 GPU 变快。

换句话说：这次修复买来的是"**服务始终可响应**"，而不是"**服务能干更多活**"。
要提升吞吐，只能从模型侧入手（见 4.5.7）。

### 4.5.6 容量规划：3060 到底能撑多少人

串行打满时每类任务的单机吞吐上限（`tests/test_capacity.py`，剔除首次模型加载）：

| 任务 | 单次耗时 | 吞吐上限 | 10 人同时排队时最后一人等待 |
|---|---|---|---|
| ASR 字幕（10 s 音频） | 1.35 s | **44.4 req/min** | 14 s |
| 口型同步（10 s 音频） | 1.37 s | **43.9 req/min** | 14 s |
| **TTS 克隆合成（约 4.7 s 音频）** | **9.60 s** | **6.2 req/min** | **96 s** |

> ASR/口型 RTF ≈ 0.13，7.6 倍实时，很宽裕。
> **TTS 是绝对瓶颈**，吞吐只有 ASR 的 1/7，根因是 dual-AR 自回归解码 RTF ≈ 1.95。

**按场景估算**（假设每人每分钟发起 1 次请求）：

| 场景 | 负载构成 | 估算并发上限 | 说明 |
|---|---|---|---|
| 纯字幕转写 | 100% ASR | **~40 人** | 很宽裕，3060 绰绰有余 |
| 纯口型驱动 | 100% 口型 | **~40 人** | 同上 |
| 纯语音克隆 | 100% TTS | **~6 人** | 第 6 人之后开始明显排队 |
| 混合（本次压测） | 40/30/15/15 | **~18 人** | 实测吞吐 17.8 req/min |
| 交互式对话（TTS 密集） | 60% TTS | **~10 人** | 超过后等待时间迅速恶化 |

**建议**：以 TTS 为主的场景，**单卡 3060 的安全并发上限约为 6 人**。
若需要更多，方案按性价比排序：

1. **降低 TTS 采样/长度**：`chunk_length` 调小、`max_new_tokens` 限制到 512，直接缩短单次耗时
2. **加一层结果缓存**：相同 (文本, 音色) 的合成结果缓存复用，命中率高时可数倍提升有效吞吐
3. **异步任务化**：改同步 HTTP 为「提交任务 → 轮询/回调」，避免长连接占用与客户端超时
4. **换卡或多卡**：TTS 是算力瓶颈，A100 / 4090 可把 RTF 降到 0.5 以下

### 4.5.7 显存观测的一个盲点（需要提醒）

压测中显存峰值只显示 ~1.7 GB，似乎远低于 8.7 GB 预算。
**这不是真实情况** —— `vram_stats()` 用的是 `torch.cuda.memory_allocated()`，
只能统计 **PyTorch 分配器**的显存；而 ASR 用的 **CTranslate2 有自己的显存池，
完全不在 PyTorch 统计范围内**（这也是为什么 ASR 测试里显存显示 0 MB）。

真实占用应通过 `nvidia-smi` 观察。这不影响本次结论（未发生 OOM，
`USS_GPU_SLOTS=2` 的额度限制生效），但**监控告警不要只看 `/v1/status` 的 vram 字段**。

---

## 5. 服务端验证

服务已启动并逐端点实测通过（监听 `http://127.0.0.1:8080`）：

| 端点 | 方法 | 验证结果 |
|---|---|---|
| `/health` | GET | `{"status":"ok","cuda":true}` |
| `/v1/status` | GET | 显存预算、常驻模型、配置快照正常 |
| `/v1/asr` | POST | 30 条测试全部返回 SRT/VTT/JSON |
| `/v1/tts` | POST | `used_reference: true`，克隆生效 |
| `/v1/voices` | GET | 正确列出 `data/voices/` 下注册音色 |
| `/v1/speaker/similarity` | POST | 同人 0.814 / 跨人 0.071 |
| `/v1/lipsync` | POST | 75 个 viseme 事件 + VTT + MP4 |
| `/v1/pipeline` | POST | 全链路 ASR→克隆→口型→MP4 打通 |
| `/v1/models/unload` | POST | 显存回收正常 |
| `/v1/output/{job}/{name}` | GET | 产物下载正常 |

**全链路实测**（`/v1/pipeline`，10.38 s 参考音频）：

```json
{"asr":    {"text":"虚拟实地考察是高科技的解决方案…", "rtf":0.169},
 "clone":  {"duration":2.136, "rtf":2.405, "sample_rate":44100},
 "lipsync":{"count":18, "duration":2.136},
 "files":  {"srt":…, "cloned_wav":…, "visemes":…, "mp4":…}}
```

---

## 6. 遇到的失败与异常（完整清单）

| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | ECAPA 加载 `KeyError: 'starting_index'` | 目录残留 **0 字节** `label_encoder.ckpt`（早期失败 symlink 产物）；speechbrain 的 `fetch()` 只要目标文件存在就短路返回 | `_local_files_ok()` 扫描删除所有 0 字节文件；预复制 `label_encoder.txt` → `.ckpt` |
| 2 | 联网把已下载的 ckpt 覆盖成 0 字节 | speechbrain `fetch()` 默认 `allow_updates=True`，source 为 HF repo id 时每次联网比对 | 生成 `hyperparams_local.yaml`，把 `pretrained_path` 改本地绝对路径，使 `guess_source()` 判为 LOCAL + `HF_HUB_OFFLINE=1` |
| 3 | Windows symlink 无权限产生 0 字节文件 | `os.symlink` 需管理员权限 | 改用 `shutil.copyfile` |
| 4 | `soundfile.LibsndfileError: Error opening 'tensor([[0.0000e+00...]])'` | `_load_wav()` 只认 numpy，传 Tensor 会走进 `torchaudio.load(str(tensor))` | 新增 `torch.Tensor` 分支 |
| 5 | `wave.Error: unknown format: 3` | `wave` 模块只支持 PCM int16，读 float32 wav 失败 | 改用 `soundfile` |
| 6 | `AttributeError: module 'imageio' has no attribute 'get_writer'` | imageio 目录只剩 `config/core/plugins`，无 `__init__.py`，被当成空命名空间包（PEP 420 陷阱） | `rm -rf` 后重装 imageio 2.37.4 |
| 7 | `ImportError: cannot import name 'Style' from 'pypinyin'` | 同上，pypinyin 目录只剩 `contrib/seg/style` | 重装 pypinyin 0.55.0 |
| 8 | 10 个依赖包目录被清空（fastapi/jinja2/networkx/cffi/anyio 等） | 同一 PEP 420 陷阱：`import` 看似成功（`__file__=None`）实则空壳 | `--no-deps --ignore-installed` 批量重装，16/16 校验通过 |
| 9 | ASR 段起点系统性偏早 −370 ms | faster-whisper 默认 `speech_pad_ms=400` 前置填充静音 | 改为 30 ms → −0.7 ms |
| 10 | 口型边界 F1 恒接近 0 | 指标粒度失配（音节级 vs 语句级） | 改为帧级一致性 F1 |
| 11 | 口型收口偏早 186 ms | Whisper 末词 end 不含能量衰减尾巴 | `_extend_tail()` 延展 → −73.8 ms |
| 12 | `TypeError: VadOptions.__init__() got unexpected keyword 'threshold'` | VadOptions 无此字段 | 查 `dataclasses.fields()` 确认后去掉 |
| 13 | `/v1/tts` 返回 `used_reference: false` | 端点只接受上传文件，`reference_audio_path` 被静默忽略；`reference_id` 是死参数 | 新增 `reference_audio_path` 与音色注册表 `_resolve_voice()` |
| 14 | 未注册音色 id 返回 500 而非 404 | 全局 `except Exception` 吞掉了 `HTTPException` | 5 个端点加 `except HTTPException: raise` 透传 |
| 15 | `/v1/pipeline` 报 `RecursionError` | **speechbrain LazyModule 重入死循环**（见下） | 加 `_patch_speechbrain_lazy()` 重入锁 |

### 6.1 重点剖析：speechbrain 的 RecursionError（第 15 项）

这是本次调试中最隐蔽的一个问题。触发条件是**先访问过任何 speechbrain 惰性模块、再触发一次深度 torch 导入**（`/v1/pipeline` 恰好是这个顺序）。

**成因链**：

1. speechbrain 把已废弃的 `speechbrain.pretrained` 在 `sys.modules` 里替换成一个 `DeprecatedModuleRedirect` 代理（惰性加载）
2. 任何对该模块的属性访问都走 `LazyModule.__getattr__` → `ensure_module()`
3. `ensure_module()` 为定位调用者，调用 `inspect.getframeinfo()`
4. 而 `getframeinfo` → `findsource` → `getmodule` 会**遍历 `sys.modules` 并对每个模块取 `__file__`**
5. 于是又碰到这个代理 → 回到步骤 2，**无限递归**

speechbrain 自己有道守卫试图阻断：

```python
if importer_frame.filename.endswith("/inspect.py"):
    raise AttributeError()
```

但这道守卫在两种情况下失效，**本项目（Windows + embeddable Python）两条全中**：

1. Windows 路径分隔符是 `\`（`D:\...\inspect.py` 不以 `/inspect.py` 结尾）
2. embeddable/zip 标准库里 `co_filename` 是裸的 `inspect.py`（没有前导 `/`）

**修复**：给 `ensure_module` 加一层重入锁，递归进来时若模块未加载完直接抛 `AttributeError`（与上游守卫行为一致），
已加载则直接返回缓存模块。这样无论路径长什么样都不会递归，已在服务启动钩子里统一应用。

---

## 7. 限制说明

| 能力 | 限制 | 影响 |
|---|---|---|
| **ASR** | 句级完全匹配率仅 56.7 % | 同音字/数字规范化差异导致；字幕可读性不受影响，但不适用于需逐字一致的场景（如字幕与出版稿自动对齐） |
| **ASR** | 时间戳为**段/词级**，非音素级 | 端到边界误差绝对均值 ~90 ms，不足以驱动严格的音素级动画 |
| **克隆** | RT F ≈ 1.95（慢于实时） | 3060 上 dual-AR 自回归解码的固有代价，不适合实时对话 |
| **克隆** | 近邻男声易混淆（cluster2 被误判为 cluster3） | 音色相似度依赖说话人本身的可辨识度；录音条件相近的同类音色区分度不足 |
| **克隆** | 需要 10 s 参考音频 + 对应转写文本 | 无转写文本时 `prompt_text` 缺失，克隆会静默退化 |
| **口型** | 收口仍偏早 73.8 ms（最差 216 ms） | 句末拖长音/气声区间口型提前闭合 |
| **口型** | 输出为**规则驱动的 9 宫 viseme**，非真人视频口型 | 适用于卡通/虚拟形象；真人视频需接 Wav2Lip，当前未集成权重 |
| **口型** | 口型切换率 5.57 次/秒 > 实际音节率 3.27 字/秒 | 声母/韵母拆分会产生比真实音节更多的口型切换，快速语流下可能略显"抖" |
| **通用** | 三模型峰值合计 ~8.5 GB | 12 GB 卡上需与桌面程序共存，依赖 LRU 回迁调度；同时高并发会触发降级 |
| **并发** | **吞吐上限不随并发提升**（~18 req/min） | 单卡 GPU 算力是硬上限；增加并发只拉长队列，不提升吞吐 |
| **并发** | TTS 场景并发上限仅 ~6 人 | dual-AR 自回归 RTF 1.95，10 人排队时最后一人需等 96 s |
| **并发** | 同步 HTTP 长连接 | 单个 TTS 请求 ~9.6 s，客户端需设置足够超时；未做异步任务化 |
| **监控** | `/v1/status` 的 vram 字段不含 CTranscate2 显存 | ASR 显存对 PyTorch 不可见，监控告警需结合 `nvidia-smi` |

---

## 8. 改进建议

### 8.1 最高优先级：吞吐（这是承载多人的唯一出路）

0. **TTS 解码加速** —— 吞吐瓶颈 100% 在 TTS（6.2 req/min vs ASR 44.4）。
   按性价比排序：① 结果缓存（相同文本+音色直接复用，命中率高时数倍提升）；
   ② `torch.compile` + CUDA graph（Linux 稳定，Windows 需验证 triton）；
   ③ 限制 `max_new_tokens=512` 缩短单次耗时。目标 RTF < 1.0 实现实时。

0. **异步任务化** —— 当前同步 HTTP 下，一个 9.6 s 的 TTS 请求会长时间占用连接。
   改为「提交任务 → 返回 job_id → 轮询/SSE 推送进度」，可消除客户端超时、
   支持排队可视化，并为后续批处理留出空间。并发压测已证明排队不可避免，
   那么把排队**显式化、可观测**就是必须做的一步。

0. **请求分级** —— 已实现的分组信号量可以再细化：给交互式请求更高优先级，
   批量离线任务低优先级，避免批量任务饿死在线用户。

### 8.2 高价值（投入产出比高）

1. **ASR 后接领域语言模型 / 热词表**
   错误几乎全是同音字替换。接入 `initial_prompt` 或 kenlm 语言模型（中文领域词表）可显著降低 CER。
   faster-whisper 支持 `hotwords` 参数，对专有名词效果立竿见影。

2. **克隆前做参考音频质量预检**
   在注册音色时用 ECAPA 计算该说话人与**已注册音色**的簇间相似度，若 > 0.45 就提示"音色可能混淆"。
   cluster2 的失败属于这一类，完全可以在注册阶段提前预警。

3. **口型：用强制对齐替代 Whisper 时间戳**
   当前收口误差的根源是 Whisper 时间戳只到"可辨识语音"。
   换用 **MFA（Montreal Forced Aligner）** 或 **WhisperX 的 wav2vec2 强制对齐**，
   可得到音素级边界，从根本上解决收口提前问题，预计能把 offset 偏差压到 ±20 ms 内。

### 8.3 中价值

4. **补齐显存观测**：接入 `pynvml` 读取进程级真实显存，让 `/v1/status`
   反映包括 CTranscate2 在内的完整占用，避免监控盲点。

5. **口型平滑**：当前 5.57 次/秒的切换率高于真实音节率 3.27。
   可在 viseme 时间轴上加一层**最小持续时间约束**（如 < 80 ms 的口型并入邻居），降低抖动感。

6. **流式 ASR**：当前是整段处理。faster-whisper 支持流式 chunk，
   接 WebSocket 可做实时字幕，对长音频场景体验提升明显。

### 8.4 架构层面

7. **显式模型预热接口**：首次请求会触发模型加载（TTS 约 8 s），
   建议加 `POST /v1/models/warmup?models=asr,tts`，在服务启动后主动预热。
   压测中这一步已手工完成，生产环境应自动化。

8. **多卡 / 多实例横向扩展**：单卡吞吐上限已测死（~18 req/min 混合负载）。
   若需线性扩容，应部署多实例 + 负载均衡，而非在单进程内加并发
   （已证明无效：并发 8 的效率仅 13%）。

9. **压测覆盖度补充**：本次并发压测的请求数为 32/级、并发上限 8。
   正式部署前建议补：① 更高并发（16/32）验证队列不崩溃；
   ② 长时间稳定性（30 min 以上）验证显存无泄漏与模型无颠簸；
   ③ 极端长文本 TTS 验证 `max_new_tokens` 上界的有效性。

---

## 9. 复现方式

```bash
# 1) 启动服务
./.tools/python311/python.exe -m service.server
# 监听 http://127.0.0.1:8080

# 2) 跑三项测试
./.tools/python311/python.exe -m tests.test_asr            # 30 条 → outputs/asr/report.json
./.tools/python311/python.exe -m tests.test_voice_clone    # 4 人   → outputs/clone/report.json
./.tools/python311/python.exe -m tests.test_lipsync        # 30 条 → outputs/lipsync/report.json
```

每份 `report.json` 含 `summary`（汇总指标）、`details`（逐样本明细）、`vram`（显存快照）三部分，
本报告的表格均由这些 JSON 直接生成，可核对。
