# 性能分析报告 —— RTX 3060 上的语音处理服务

> 测试日期：2026-09-01 · 硬件：NVIDIA GeForce RTX 3060 12GB
> 聚焦两件事：**单任务有多快**、**多人同时用能撑多少人**

---

## 0. 一句话结论

**服务质量达标，但单卡吞吐是硬伤。**

- 三项功能**精度**都很好（字准确率 97.6%、音色相似度 0.589、口型帧 F1 0.939）
- 但 **TTS 慢于实时**（RTF 1.95），是整条链路的绝对瓶颈
- 混合负载下**吞吐上限约 18 req/min，且不随并发提升** —— 这是单卡算力天花板
- 以 TTS 为主的场景，**安全并发上限只有约 6 人**
- 已修复一个会导致服务"假死"的严重并发缺陷（`/health` 延迟 31 s → 104 ms）
- **已实施 TTS 结果缓存**：固定话术场景命中率 75% 时，吞吐 **5.8 → 27.8 req/min（4.84x）**，
  平均延迟 10.44 s → 2.15 s。这是绕过算力限制唯一有效的低成本手段（见 [§6](#6-tts-结果缓存已实施)）

详细的「优化该做什么」直接跳到 [§7](#7-优化建议按-roi-排序)。

---

## 1. 测试环境

| 项目 | 值 |
|---|---|
| GPU | NVIDIA GeForce RTX 3060（12.0 GB，Ampere sm_86，bf16 原生） |
| CPU / OS | Windows 10 x64 |
| Python | 3.11.9（embeddable，64 bit） |
| PyTorch | 2.6.0+cu124（CUDA 12.4 / cuDNN 9.1.0） |
| faster-whisper | 1.1.0（CTranslate2 4.5.0） |
| speechbrain | 1.0.2 |
| FishSpeech | 1.5（dual_ar LLaMA + Firefly VQGAN，44.1 kHz） |
| 服务 | FastAPI + uvicorn，**单进程单 worker**，`127.0.0.1:8080` |

---

## 2. 测试方法

### 2.1 回答你的问题：压测是**三项功能混合**的

不是只测一项。分两层：

**第一层 —— 混合并发压测**（`tests/test_concurrency.py`）

按贴近真实使用的比例混合发送，模拟"多人多任务"：

| 任务 | 占比 | 说明 |
|---|---|---|
| ASR 字幕转写 | **40%** | 最常用 |
| TTS 克隆合成 | **30%** | 最重 |
| 口型同步 | **15%** | 含 ASR + 时间轴（渲染关闭以聚焦 GPU） |
| `/health` 探针 | **15%** | 用于观察轻请求是否被饿死 |

- 并发级别：**1 / 2 / 4 / 8**（N 个线程 = N 个虚拟用户）
- 每级 32 个请求，**各并发级使用同一份预生成的任务序列**，保证横向可比
- 8 个不同音频文件轮询，模拟不同用户上传不同内容
- 后台每 0.5 s 采样一次显存

**第二层 —— 单项容量测试**（`tests/test_capacity.py`）

单线程**串行打满**每一类任务（无排队干扰），测出该项的**理论吞吐上限**，
用来定位"到底是谁在拖后腿"。每项重复 8 次，剔除首次模型加载。

### 2.2 采集的指标

| 指标 | 定义 |
|---|---|
| 吞吐 | 完成请求数 / 墙钟（req/min） |
| 排队时间 | 端到端延迟 − 服务端自报处理时间（**逐条配对**做差，不是两组均值相减） |
| 加速比 | N 并发吞吐 / 单并发吞吐；理想值 = N |
| 轻任务饿死比 | 并发下 `/health` 延迟 / 空载基线延迟 |
| RTF | 处理耗时 / 音频时长（< 1 表示快于实时） |

---

## 3. 单项性能基准

### 3.1 串行打满时的吞吐上限（核心数据）

| 任务 | 单次耗时 | 服务端处理 | 吞吐上限 | RTF | 相对 ASR |
|---|---|---|---|---|---|
| **ASR 字幕**（10 s 音频） | 1.35 s | 1.13 s | **44.4 req/min** | 0.13 | 1.0x |
| **口型同步**（10 s 音频，不渲染） | 1.37 s | — | **43.9 req/min** | — | 1.0x |
| **TTS 克隆合成**（约 4.7 s 音频） | **9.60 s** | 9.38 s | **6.2 req/min** | **1.95** | **0.14x** |

> **TTS 的吞吐只有 ASR 的 1/7。** 整条链路的容量由它决定。

### 3.2 三项功能的质量与资源

| 能力 | 精度指标 | 实测 | 显存 | 判定 |
|---|---|---|---|---|
| ASR | 字准确率（1−CER） | **97.59 %** | 3.2 GB | 优秀 |
| ASR | 段起点偏差 | **−0.7 ms** | | 优秀 |
| 克隆 | 音色相似度（留出段） | **0.589**（阈值 0.55） | 1.65 GB | 合格 |
| 克隆 | 区分度 margin | **+0.371** | | 优秀 |
| 克隆 | 合成内容准确率 | **100 %** | | 优秀 |
| 口型 | 帧级一致性 F1 | **0.939** | — | 良好 |
| 口型 | 起口 / 收口偏差 | **−5.5 ms** / −73.8 ms | | 良好 / 待改进 |

注：ASR 峰值显存 3.2 GB 为 large-v3 fp16 权重 + 编码激活；克隆 1.65 GB 为 TTS 常驻。

---

## 4. 并发性能（混合三项功能）

### 4.1 修复前：服务对外表现为"卡死"

| 并发 | 吞吐 | 加速比 | 效率 | P50 | P95 | 排队均值 | `/health` | 饿死比 | 成功率 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 17.66 | 1.00x | 100% | 1.35 s | 9.91 s | 0.23 s | 3 ms | 0.6x | 100% |
| 2 | 17.06 | 0.97x | 48% | 3.17 s | 19.98 s | 3.52 s | **1 318 ms** | **260x** | 100% |
| 4 | 20.39 | 1.15x | 29% | 12.15 s | 24.32 s | 9.37 s | **17 939 ms** | **3 534x** | 88% |
| 8 | 18.99 | 1.08x | 13% | 22.18 s | 48.86 s | 20.07 s | **31 027 ms** | **6 113x** | 84% |

两个致命信号：

1. **吞吐不随并发提升**（17.7 → 19.0，基本持平）。并发 8 的效率**只有 13%**。
2. **`/health` 探针要等 31 秒**（饿死 6 113 倍）。GPU 一直在干活，
   但服务对外**完全无响应** —— 心跳会失败、负载均衡会摘除节点、前端显示"服务不可用"。

### 4.2 根因：`async def` 里的同步调用独占了事件循环

FastAPI 的 `async def` 端点跑在 asyncio 事件循环上。
**在 `async def` 里直接调用同步阻塞函数，会独占整个事件循环。**

原代码正是这个形态：

```python
@app.post("/v1/tts")
async def api_tts(...):
    engine = clone_mod.get_tts()      # 同步，可能触发数秒模型加载
    res = engine.synthesize(...)      # 同步，~9 秒，期间事件循环完全冻结
```

延迟数据也印证了这点 —— 并发 4 时呈 `1.83 / 3.72 / 5.44 / 7.25 s` 的**等差阶梯**，
公差恰好等于单请求耗时，说明请求被逐个串行处理。

### 4.3 修复：线程池卸载 + 按模型分组串行

新增 `service/concurrency.py`：

```python
async def run_gpu(kind, fn, *args, **kwargs):
    async with _total_sem():          # 全局额度，防显存超限
        async with _group_sem(kind):  # 同类模型串行，异类可并行
            return await run_in_threadpool(fn, *args, **kwargs)
```

| 原则 | 做法 | 理由 |
|---|---|---|
| 不阻塞事件循环 | `run_in_threadpool` | 轻请求不再被重请求饿死 |
| 同类模型串行 | `asr`/`tts`/`speaker` 信号量 = 1 | 同一实例并发推理会共享 CUDA context 与 KV cache，会出错 |
| 异类模型并行 | 全局额度 `USS_GPU_SLOTS=2` | 不同模型显存区独立可重叠；额度限制防 OOM |

### 4.4 修复后对比

| 并发 | 吞吐 前→后 | P50 前→后 | `/health` 前→后 | 饿死比 前→后 | 成功率 前→后 |
|---|---|---|---|---|---|
| 1 | 17.7 → 17.9 | 1.35s → 1.36s | 3ms → 1ms | 0.6x → 0.3x | 100% → 100% |
| 2 | 17.1 → 17.6 | 3.17s → **2.03s** | 1 318ms → **2ms** | 260x → **0.4x** | 100% → 100% |
| 4 | 20.4 → 17.6 | 12.15s → **8.38s** | 17 939ms → **15ms** | 3 534x → **4.0x** | 88% → **100%** |
| 8 | 19.0 → 17.8 | 22.18s → 21.31s | 31 027ms → **104ms** | 6 113x → **27.8x** | 84% → **100%** |

**诚实解读**：

- ✅ **响应性问题彻底解决**：`/health` 从 31 027 ms 降到 104 ms（**298 倍**），
  成功率 84% → 100%。服务不再"假死"。
- ❌ **吞吐上限没有提升**（~18 req/min 不变）—— 这是**物理上限，不是 bug**。
  单张 3060 的算力就这么多，推理必须串行，加并发只会让队列变长，不会让 GPU 变快。

> ⚠️ 一处数据需要说明：修复后 N=4 的 P95（31.63 s）反而略高于修复前（24.32 s）。
> 这是因为修复前有 12% 的请求**快速失败**（客户端 ProxyError），把高分位拉低了；
> 修复后 100% 成功，P95 反映的是真实分布。**P50 与成功率才是可比指标。**

这次修复买来的是「**服务始终可响应**」，而不是「**服务能干更多活**」。
要提升吞吐，只能从模型侧入手。

---

## 5. 容量规划：3060 到底能撑多少人

### 5.1 按负载构成估算

基于实测吞吐上限，假设**每人每分钟发起 1 次请求**：

| 场景 | 负载构成 | 无缓存 | 缓存命中率 75% | 10 人排队时最后一人等待 |
|---|---|---|---|---|
| 纯字幕转写 | 100% ASR | **~40 人** | ~40 人（缓存不适用） | 14 s |
| 纯口型驱动 | 100% 口型 | **~40 人** | ~40 人（缓存不适用） | 14 s |
| **纯语音克隆**（文本不重复） | 100% TTS | **~6 人** | ~6 人 | **96 s** |
| **纯语音克隆**（固定话术） | 100% TTS | ~6 人 | **~27 人** | 21 s |
| 混合（本次压测） | 40/30/15/15 | **~18 人** | 更高 | — |
| 交互式对话（TTS 密集） | 60% TTS | **~10 人** | 视重复率 | — |

> 换算方法：并发上限 = 吞吐上限(req/min) ÷ 单人请求频率(次/min)。
> 若每人每 30 秒请求一次，上表人数减半。

### 5.2 关键判断

- **ASR 和口型非常宽裕**（44 req/min），3060 跑这两项绰绰有余
- **TTS 是唯一瓶颈**，纯 TTS 场景 6 人之后开始明显排队
- **缓存能把固定话术场景的容量从 6 人拉到约 27 人**（4.84x），
  但**只对重复文本有效** —— 实时对话这类文本唯一的场景收益为 0
- 排队是不可避免的，所以**把排队显式化**（异步任务 + 进度反馈）比消除排队更现实

---

## 6. TTS 结果缓存（已实施）

单卡算力是硬上限，加并发无效（并发 8 效率仅 13%）。
**缓存是绕过算力限制的唯一低成本手段** —— 命中即完全跳过 GPU 推理。

### 6.1 实现

`service/tts_cache.py`，已在 `/v1/tts` 接入：

```
key = sha256(文本 | 参考音频内容 | 参考文本 | 解码参数)
值  = 已合成的 wav，落在 data/tts_cache/<key>.wav
淘汰 = LRU（按访问时间），默认上限 500 条
```

几个关键设计：

| 设计 | 理由 |
|---|---|
| 参考音频按**内容**摘要而非路径 | 文件改名/移动不影响命中；内容变了一定不误命中 |
| 解码参数（温度/top_p/seed 等）参与 key | 参数不同视为不同结果，不会串味 |
| 写入用「临时文件 + 原子替换」 | 并发下其它请求不会读到写了一半的 wav |
| 响应新增 `cached` 字段 | 便于客户端与压测脚本识别命中情况 |

### 6.2 实测收益（`tests/test_cache_benefit.py`，12 次请求/模式，测前清空缓存）

| 场景 | 命中率 | 平均延迟 | 吞吐 | 提升 |
|---|---|---|---|---|
| 全部唯一文本（最坏情况） | 0% | 10.44 s | 5.8 req/min | 1.00x |
| **固定话术循环（典型场景）** | **75%** | **2.15 s** | **27.8 req/min** | **4.84x** |

> 命中时 `latency_s` 为 0.0，延迟仅剩文件复制与 HTTP 开销。

**关键判断**：缓存的价值完全取决于**业务里的文本重复率**。
客服话术、播报、批量配音这类场景重复率很高，收益接近 5 倍；
若每段文本都独一无二（如实时对话），缓存收益为 0，只能靠解码加速。

### 6.3 使用

```bash
# 查看命中率（衡量多人场景有效吞吐的关键指标）
curl -s http://127.0.0.1:8080/v1/cache/stats
# {"enabled":true,"entries":42,"size_mb":13.7,"hits":9,"misses":3,"hit_rate":0.75}

# 清空缓存（压测前调用，保证测的是真实合成耗时）
curl -s -X POST http://127.0.0.1:8080/v1/cache/clear
```

环境变量：

| 变量 | 默认 | 说明 |
|---|---|---|
| `USS_TTS_CACHE` | `1` | 设为 `0` 关闭缓存 |
| `USS_TTS_CACHE_MAX` | `500` | LRU 上限条数 |

`/v1/status` 的 `tts_cache` 字段也会返回命中率，便于接入监控。

---

## 6.5 TTS 解码瓶颈剖析（`tests/profile_tts.py`）

TTS 是整条链路的瓶颈，值得单独弄清楚**它到底慢在哪**。
以下为实测数据，不是推测。

### 基础测量

| 项 | 实测值 |
| --- | --- |
| 实测显存带宽（512 MB buffer 反复拷贝） | **267 GB/s** |
| LLM 参数量 / 权重体积 | 637.9 M / **1 217 MB**（bf16） |
| 生成速率 | **9.8 tokens/s**（每 token 102 ms） |
| 每 token 需读取的权重 | ~1.2 GB |
| **有效带宽** | **~12 GB/s，仅为实测带宽的 4.5%** |
| 参考音频编码（一次性） | 338 ms |
| 输出 RTF | 2.30 |

> ⚠️ 官方日志里的 `Bandwidth achieved: 6.25 GB/s` **不是真实显存带宽**，
> 它的定义是 `model_size × tokens_sec`（每秒流过权重的字节数），是内存流量的代理指标。
> 别拿它跟 360 GB/s 的标称带宽比 —— 会得出错误结论。

### kernel 级剖析（`--profile`）

用 `torch.profiler` 统计（注意：profiler 本身带来约 3.7x 开销，
绝对数值被放大，但**比例关系可靠**）：

| 算子 | 自用 CUDA 时间 | 占比 | 调用次数 |
| --- | --- | --- | --- |
| `aten::copy_` | 2 475 ms | 8.95% | 76 296 |
| `aten::mul` | 2 154 ms | 7.79% | 87 905 |
| `aten::as_strided` | 1 911 ms | 6.91% | **277 087** |
| `aten::mm` | 1 672 ms | 6.05% | 26 596 |
| `aten::_to_copy` | 1 527 ms | 5.53% | 60 924 |
| `aten::matmul` | 1 415 ms | 5.12% | 36 853 |
| `aten::select` | 1 168 ms | 4.23% | 86 904 |
| `aten::view` | 865 ms | 3.13% | **128 604** |

按算子性质归类（92 个 token）：

| 类别 | 耗时 | 占比 | 调用次数 |
| --- | --- | --- | --- |
| 计算（mm/bmm/matmul/mul/add） | 6 750 ms | ~24% | 240 k |
| **搬运/拷贝**（copy_/_to_copy/to/cat） | 4 790 ms | **~17%** | 225 k |
| **视图/整形**（as_strided/view/reshape/select/transpose） | 7 620 ms | **~28%** | 675 k |
| 其它 | — | ~31% | — |

### 结论：瓶颈是「大量碎小算子」，不是算力

- 每个 token 触发 **约 11 000 次 kernel launch**，平均每次仅几 µs
- **非计算类开销（搬运 + 视图整形）合计约 45%**，几乎与真正的矩阵运算相当
- 这解释了为什么显存带宽只跑到 4.5%：GPU 大部分时间在等 launch、做小拷贝和整形，
  而不是在搬大块数据

### 为什么常见的优化手段在这里不奏效

| 手段 | 可用性 | 说明 |
| --- | --- | --- |
| `torch.compile` | ❌ **不可用** | Windows 官方 wheel **不含 triton**，inductor 后端无法工作 |
| CUDA Graph | ⚠️ 理论可行 | 对 launch 受限负载是正解，但 dual-AR 每步 `input_pos` 变化，需静态 buffer + 拷贝，改动 FishSpeech 官方解码循环，风险较高 |
| 降精度（fp16/int8） | ⚠️ 收益有限 | 瓶颈不在算力也不在权重体积，而在算子数量 |
| 减少 `_to_copy` | 🤔 待验证 | 12 万次 dtype 转换（采样路径）值得单独看 |

**务实结论**：在 Windows + 3060 这个组合上，把 TTS 从 RTF 1.95 压到 1.0 以内
需要动 FishSpeech 的解码循环（CUDA Graph 或手工算子融合），这属于深度定制，
风险与工作量都不小。**当前更划算的路径仍是缓存 + 异步任务化**（已完成的两项）。

---

## 7. 显存分析

### 6.1 预算与实际

| 模型 | 常驻显存 | 说明 |
|---|---|---|
| FishSpeech TTS | 3.6 GB | dual_ar 1.2 GB 权重 + FireflyVQGAN + KV cache |
| Whisper large-v3 fp16 | 3.2 GB | CTranslate2 分配 |
| ECAPA-TDNN | 0.7 GB | 说话人编码 |
| **合计** | **7.5 GB** | 预算 8.5 GB，留出余量给桌面程序与峰值激活 |

压测全程**未发生 OOM**，最低空闲显存 5.3 GB，`USS_GPU_SLOTS=2` 的额度限制生效。

### 6.2 ⚠️ 一个监控盲点（重要）

压测中 `/v1/status` 显示的显存峰值只有 **~1.7 GB**，看似远低于预算。
**这不是真实情况。**

`vram_stats()` 用的是 `torch.cuda.memory_allocated()`，只能统计 **PyTorch 分配器**的显存；
而 ASR 用的 **CTranslate2 有自己的显存池，完全不在 PyTorch 统计范围内**
—— 这也是为什么 ASR 测试里显存显示 0 MB。

**结论**：`/v1/status` 的 vram 字段**不能用于容量监控**，
真实占用需通过 `nvidia-smi` 或接入 `pynvml` 读取进程级显存。

---

## 8. 优化建议（按 ROI 排序）

### ✅ 已完成：TTS 结果缓存

吞吐瓶颈 100% 在 TTS（6.2 vs 44.4 req/min）。已实现并验证：
固定话术场景命中率 75% 时吞吐 **5.8 → 27.8 req/min（4.84x）**，平均延迟 10.44 s → 2.15 s。
详见 [§6](#6-tts-结果缓存已实施)。

**落地建议**：上线后监控 `/v1/cache/stats` 的命中率。
若命中率 > 40%，缓存已在显著改善容量；若长期 < 10%，
说明业务文本高度唯一，应把精力转向解码加速而非扩容缓存。

### ✅ 已完成：异步任务化

并发压测已证明排队不可避免（TTS 吞吐 6.2 req/min），
那么把排队**显式化、可观测**就是必须做的一步。已实现 `service/jobs.py`：

```
POST   /v1/jobs            提交任务，立即返回 job_id（毫秒级）
GET    /v1/jobs/{id}       查询状态 + 队列位置 + 结果
GET    /v1/jobs            列出最近任务 + 统计
DELETE /v1/jobs/{id}       取消（对 pending / running 有效）
```

同步端点保持不变，两者共用 `_do_asr` / `_do_tts` / `_do_lipsync` 实现，行为一致。

**冷路径实测**（4 个 TTS 任务，清空缓存，观察排队）：

| 时刻 | 任务1 | 任务2 | 任务3 | 任务4 |
| --- | --- | --- | --- | --- |
| 0.0 s | running (pos 0) | running (pos 0) | pending (pos 0) | pending (pos 1) |
| 10.0 s | **done** | running | running | pending |
| 20.1 s | done | **done** | running | running |
| 40.1 s | done | done | done | **done** |

端到端：9.57 / 19.39 / 31.75 / 40.42 s（TTS 串行排队的阶梯，符合预期）。

**价值不在变快，而在可控**：

- 客户端提交即返回，**无需设置 40 秒超时**，也不会被代理 / 网关提前切断
- `queue_pos` 让前端能显示「前面还有 N 个」，体验远好于干等
- 任务保留 1 小时，结果文件始终在磁盘上

**设计取舍**：用 asyncio + 内存队列，不引入 celery / redis —— 单进程服务够用、零外部依赖。
代价是**服务重启后任务列表丢失**（结果文件仍在）。若需多实例或持久化，再换 arq + redis。


### 🥈 第二优先：TTS 解码加速（先读 §6.5 的剖析结论）

剖析已定位瓶颈是「每 token 约 11 000 次碎小 kernel」，非算力不足。
因此**不要**优先尝试降精度或减权重 —— 收益有限。

| 手段 | 可用性 | 说明 |
| --- | --- | --- |
| `torch.compile` | ❌ 已验证不可用 | Windows 官方 wheel 不含 triton |
| CUDA Graph | ⚠️ 推荐但工作量大 | 对 launch 受限是正解；需改 dual-AR 解码循环，用静态 buffer 承接动态 `input_pos` |
| 减少采样路径的 dtype 转换 | 🤔 可先试 | 12 万次 `_to_copy`，投入小，可先量化收益 |



| 手段 | 预期 | 备注 |
|---|---|---|
| `torch.compile` + CUDA graph | RTF 1.95 → ~1.0 | Linux 稳定；Windows 需验证 triton |
| `max_new_tokens` 降到 512 | 单次耗时显著下降 | 限制长文本，需业务可接受 |
| `chunk_length` 调小 | 降低 KV cache 峰值 | 已设 150 |

目标是 RTF < 1.0 实现实时。

### 🥉 第三优先：补齐监控

接入 `pynvml` 读取进程级真实显存，让 `/v1/status` 反映包括 CTranslate2 在内的完整占用。
当前只看 PyTorch 分配器会有严重低估。

### 第四优先：横向扩展

单卡吞吐已测死（~18 req/min 混合负载，加并发无效）。
若需线性扩容，应部署**多实例 + 负载均衡**，而不是在单进程内加并发
（已证明无效：并发 8 的效率仅 13%）。

---

## 9. 跨硬件对比（4070Ti / 4080 / 4090 / Apple M4）

> 3060 一列为本机实测，其余为基于规格与 §6.5 剖析结论的**推算值**（标"估"）。
> 推算依据：TTS 受限于 kernel launch 开销而非带宽（有效带宽仅 4.5%），
> 因此换卡收益 = min(带宽倍数, launch 开销占比的削减)，呈**次线性**。

| 配置 | 架构 / 显存 | 显存带宽 | TTS (req/min) | 三模型常驻 | 定性结论 |
|---|---|---|---|---|---|
| **RTX 3060 12GB（当前）** | Ampere / 12G | 267 GB/s（实测） | **6.2**（实测） | 紧绷，需调度换入换出 | 基线 |
| RTX 4070 Ti 12GB | Ada / 12G | 504 GB/s | ~9.5（估） | **紧绷，同 12G** | 单请求快 ~1.5x，并发容量无改善 |
| RTX 4080 16GB | Ada / 16G | 717 GB/s | ~12（估） | 宽松 | 速度 + 容量双改善 |
| RTX 4090 24GB | Ada / 24G | 1008 GB/s | ~15（估） | 充裕，可放大 KV cache | 最佳；上 CUDA Graph 后可达 ~24 |
| Apple M4 16GB（基础款） | 统一内存 / 16G | 120 GB/s | **不可行** | — | 无 CUDA，需移植，慢 5–10x |

### 三个关键判断

1. **TTS 是「launch 受限」而非「算力/带宽受限」—— 裸换显卡收益次线性**（§6.5）。
   4070Ti 带宽 1.9x 但 TTS 只快 ~1.5x；4090 带宽 3.8x 但 TTS 只快 ~2.4x。
   真正的倍增器是 **CUDA Graph**（消除 launch 开销，RTF 1.95 → ~1.0，见 `CUDAGRAPH_PLAN.md`），
   之后 4090 的带宽才被真正利用。
2. **显存是隐藏约束：4070Ti 仍是 12GB，与 3060 完全相同**。
   三模型常驻需 ~7.5GB（§7），12G 卡去掉桌面程序余量后调度器必须来回换入换出 ——
   **并发容量瓶颈在显存不在算力**。只有 4080(16G)/4090(24G) 能三模型全常驻。
3. **M4 不可行是生态问题而非性能问题**：服务是 CUDA 栈（PyTorch cu124 + CTranslate2），
   Apple Silicon 无 CUDA、CTranslate2 无 Metal 后端（只能 CPU），FishSpeech decode 需改走 MPS。
   即便移植，120 GB/s 还要与 macOS/系统共享 16GB，整体比 3060 慢一个数量级。

### 部署建议

- **换机不改码**：选 4080 或 4090；4070Ti 性价比最低（12G 天花板未变）。
- **不花钱先提吞吐**：优先 CUDA Graph 改造（3060 自身也能 RTF → ~1.0）。
- **Mac 部署**：暂不推荐，除非专门做 Metal/CPU 后端移植。

---

## 10. 测试覆盖度的不足

坦白说明本次压测的边界，便于判断结论的适用范围：

| 未覆盖 | 影响 |
|---|---|
| 并发上限只测到 8 | 更高并发（16/32）下队列行为未验证 |
| 口型压测关闭了渲染 | 带 MP4 渲染时单请求更慢，容量会低于 43.9 req/min |
| 未做长时间稳定性测试 | 显存泄漏、模型反复加载/颠簸未验证（建议 30 min+ 长跑） |
| 未测极端长文本 TTS | `max_new_tokens` 上界的有效性未验证 |
| 8 个音频文件轮询 | 未覆盖更长/更短音频的分布 |

---

## 11. 复现方式

```bash
# 启动服务
./.tools/python311/python.exe -m service.server

# 三项功能测试（精度指标）
./.tools/python311/python.exe -m tests.test_asr
./.tools/python311/python.exe -m tests.test_voice_clone
./.tools/python311/python.exe -m tests.test_lipsync

# 并发压测（混合三项，1/2/4/8）
./.tools/python311/python.exe -m tests.test_concurrency
./.tools/python311/python.exe -m tests.test_concurrency --quick   # 只跑 1/4

# 单项容量上限
./.tools/python311/python.exe -m tests.test_capacity

# TTS 缓存收益(对比唯一文本 vs 固定话术)
./.tools/python311/python.exe -m tests.test_cache_benefit

# 修复前后对比
./.tools/python311/python.exe -m tests.compare_concurrency
```

产物：

| 文件 | 内容 |
|---|---|
| `outputs/concurrency/report.json` | 并发压测原始数据（含逐条记录） |
| `outputs/concurrency/report.md` | 并发压测报告 |
| `outputs/concurrency/report_BEFORE.json` | 修复前基线 |
| `outputs/concurrency/capacity.json` | 单项容量上限 |
| `outputs/concurrency/cache_benefit.json` | TTS 缓存收益（4.84x） |
| `outputs/{asr,clone,lipsync}/report.json` | 三项功能精度测试 |

相关文档：功能精度详见 [REPORT.md](REPORT.md)，架构与 API 详见 [README.md](README.md)。
